{
  "capacityId": "{{ capacity_id }}",
  "client_id": "{{ client_id }}",
  "client_secret": "{{ client_secret }}",
  "tenant_id": "{{ tenant_id }}",
  "workspaces": [
    {% for ws in workspaces %}
    {
      "name": "{{ ws.name }}"{% if ws.lakehouse %}, "lakehouse": "{{ ws.lakehouse }}" {% endif %}
    }{% if not loop.last %},{% endif %}
    {% endfor %}
  ]
}